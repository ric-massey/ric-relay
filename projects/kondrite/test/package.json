{
  "name": "kondrite-tests",
  "private": true,
  "description": "Test-only. The game itself has no dependencies and no build step — see ../README.md. Playwright lives here so that fact stays true of the game directory.",
  "devDependencies": {
    "playwright": "^1.63.0"
  },
  "scripts": {
    "browser": "node browser.js",
    "setup": "npm install && npx playwright install chromium"
  }
}
